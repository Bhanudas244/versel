<!DOCTYPE html>
<html>
<head>
  <title>Email Form</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
</head>
<body>
  <div class="container">
    <h2>Email</h2>
    <form action="/sendEmails" method="post">
      <div class="form-group">
        <label for="recipients">To (Separate by comma)</label>
        <input type="text" class="form-control" id="recipients" rows="3" name="recipients" placeholder="Recipients (comma-separated)">
      </div>
      <div class="form-group">
        <label for="subject">Subject</label>
        <input type="text" class="form-control" id="subject" name="subject" placeholder="Subject">
      </div>
      <div class="form-group">
        <label for="message">Message</label>
        <textarea class="form-control" id="message" name="message" rows="3" placeholder="Message"></textarea>
      </div>
      <button type="submit" class="btn btn-primary">Send</button>
    </form>
  </div>

  <!-- <script>
    // JavaScript to handle the form submission
    document.getElementById('emailForm').addEventListener('submit', function(event) {
      event.preventDefault();
      
      // Get the value of the recipients field
      const recipientsInput = document.getElementById('recipients');
      const recipientsValue = recipientsInput.value;
  
      // Remove leading and trailing whitespaces from the input
      const trimmedValue = recipientsValue.trim();
      
      // Check if the value contains spaces
      if (trimmedValue.includes(' ')) {
        // If there are spaces, keep the email addresses as a single element in the array
        const emailArray = [trimmedValue];
        
        // Join the email array into a single comma-separated string
        const commaSeparatedEmails = emailArray.join(',');
        
        // Update the value of the recipients field with the comma-separated string
        recipientsInput.value = commaSeparatedEmails;
    
      }
      
      // Submit the form
      this.submit();
    });
  </script> -->



</body>
</html>
